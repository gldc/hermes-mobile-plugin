"""hermes_mobile — core package for the hermes-mobile plugin.

Modules:
    device_store   — pure-stdlib device registry (~/.hermes/mobile/devices.json)
    auth_provider  — MobileDeviceProvider (DashboardAuthProvider subclass;
                     imports hermes_cli at runtime inside the hermes process)
"""
